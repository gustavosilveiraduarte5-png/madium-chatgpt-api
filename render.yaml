services:
  - type: web
    name: madium-chatgpt-api
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn main:app"
    envVars:
      - key: GROQ_API_KEY
        sync: false
